@using Shopping.Data.Entities;
@model City;

@{
    ViewData["Title"] = "Details";
}

<h1>Detalles</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="EditCity" asp-route-id="@Model.Id" class="btn btn-sm btn-warning">Editar</a>
    <a asp-action="DetailsState" asp-route-id="@Model.State.Id" class="btn btn-sm btn-success">Regresar</a>
</div>
